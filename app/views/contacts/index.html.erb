<h1>My Contacts</h1>

<ul>
  <% @contacts.each do |c| %>
    <li><%= c.fname %> <%= c.lname %>
    <%= link_to "Edit this contact", edit_contact_path(c) if c.user == current_user %></li>
  <% end %>
</ul>
