<h1>Edit this touchpoint for <%= @touch.contact.fname %> <%= @touch.contact.lname %>!</h1>

<%= form_for [@user, @touch] do |f| %>
  <%= f.select :type, options_for_select(["Anniversary", "Birthday", "Solicitation", "Other"]) %>
  <%= f.text_field :description, placeholder: "description" %>
  <%= f.date_field :due_date, placeholder: "due date" %>
  <%= f.select :recurrence, options_for_select(["Never", "Every Day", "Every Week", "Every 2 Weeks", "Every Month", "Every Year"]) %>
  <%= f.text_field :notes, placeholder: "notes" %>
  <%= f.submit "Submit!" %>
<% end %>