<div class="container">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <h1 class="lighter_font text-center">Start Using <span class="text-primary">touchPOINT</span></h1>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-4 col-sm-offset-4 form_styles">
          
      <%= form_for @user, {url: users_path} do |f| %>
        <h3><small>Create a free account to get started now.</small></h3>

        <% if @user.errors.any? %>
          <div id="error_explanation">
            
            <ul class="list-group">
              <% @user.errors.full_messages.each do |msg| %>
                <li class="list-group-item list-group-item-danger"><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= f.text_field :email, placeholder: "email address", class: 'form-control', type: 'email' %>
        </div>
        <div class="form-group">
          <%= f.text_field :fname, placeholder: "first name", class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.text_field :lname, placeholder: "last name", class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, placeholder: "password", class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.password_field :password_confirmation, placeholder: "confirm your password", class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.submit "Sign up", class: 'btn btn-primary btn-large' %>
        </div>
      <% end %>
    </div>
  </div>
</div>